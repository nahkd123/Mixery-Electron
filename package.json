{
  "name": "mixery-electron",
  "version": "1.0.0",
  "description": "An Electron version of Mixery",
  "main": "bin/index.js",
  "scripts": {
    "test": "tsc -b && cd Mixery && tsc -b && cd .. && electron .",
    "build": "tsc -b && cd Mixery && tsc -b && cd .. && node bin/build",
    "run": "electron ."
  },
  "keywords": [
    "mixery"
  ],
  "author": "nahkd123",
  "license": "GPL-3.0-or-later",
  "devDependencies": {
    "electron": "^10.1.2"
  },
  "dependencies": {
    "@types/node": "^14.11.1",
    "asar": "^3.0.3"
  }
}
